name: CI/CD
on:
  workflow_dispatch:
  push:
    branches: [main]
    tags: ['*']
  pull_request:
    branches: [main]
    tags: ['*']

jobs:
  build-test-deploy-release:
    name: CI/CD
    uses: adombence/.github/.github/workflows/vite.yml@main
    secrets: inherit