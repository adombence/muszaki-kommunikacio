name: CI/CD
on:
  workflow_dispatch:
  push:
    branches: [main]
    tags: ['*']

jobs:
  build-deploy:
    name: CI/CD
    uses: adombence/.github/.github/workflows/vite.yml@main
    secrets: inherit
